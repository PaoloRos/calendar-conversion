[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "calendar-conversion"
version = "0.1.0"
description = "Convert normalized schedules into iCalendar files."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
